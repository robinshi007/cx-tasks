type Query {
  tasks: [Task!]!
  fetchTask (input: FetchTask!): Task!
}
type Mutation {
  createTask(input: NewTask!): Task!
  updateTask(input: EditTask!): Task!
  deleteTask(input: FetchTask!): Task!
}

scalar Time
