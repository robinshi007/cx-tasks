import styled from 'styled-components';

export const ProjectsWrapper = styled.div``;

const ProjectsPage = () => {
  return <ProjectsWrapper>ProjectsPage</ProjectsWrapper>;
};

export default ProjectsPage;
