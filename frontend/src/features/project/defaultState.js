// currentProjectId should be empty, here set to '1' for demo use
const defaultState = {
  currentProjectId: '1',
  filters: {},
  groupBy: 'section',
  sortBy: { field: '', direction: '' },
};
export default defaultState;
